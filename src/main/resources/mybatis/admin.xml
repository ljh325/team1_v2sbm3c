<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.admin.AdminDAOInter">
  <!-- 로그인 -->
  <select id="login" resultType="int" parameterType="dev.mvc.admin.AdminVO">
    SELECT COUNT(*) as cnt
    FROM admin
    WHERE adminid=#{adminid} AND password=#{password} 
  </select>
  
  <!-- 관리자 정보 -->
  <select id="read_by_adminid" resultType="dev.mvc.admin.AdminVO" parameterType ="String">
    SELECT adminno, adminid, password, grade 
    FROM admin
    WHERE adminid=#{adminid}
  </select>
  
  <!-- adminno를 통한 회원 정보 -->
  <select id="read" resultType="dev.mvc.admin.AdminVO" parameterType ="int">
    SELECT adminno, adminid, password, grade
    FROM admin
    WHERE adminno=#{adminno}
  </select>
  
</mapper>
